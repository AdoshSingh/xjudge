version: '3.9'

services:
  frontend:
    build:
      context: ./frontend
    container_name: xjudge_frontend
    command: ["npm", "run", "dev"]
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:9000
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    container_name: xjudge_backend
    command: ["npm", "run", dev]
    ports:
      - "9000:9000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - RABBITMQ_URL=amqp://user:password@rabbitmq:5672
      - DB_URL=mongodb://mongo:27017/xjudge
    depends_on:
      - mongo
      - rabbitmq

  worker:
    build:
      context: ./worker
    container_name: xjudge_worker
    command: ["python", "worker.py"]
    volumes:
      - ./worker:/app
      - /app/venv
    environment:
      - RABBITMQ_URL=amqp://user:password@rabbitmq:5672
      - DB_URL=mongodb://mongo:27017/xjudge
    depends_on:
      - mongo
      - rabbitmq

  mongo:
    image: mongo:latest
    container_name: xjudge_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  rabbitmq:
    image: rabbitmq:4-management
    container_name: xjudge_rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=user
      - RABBITMQ_DEFAULT_PASS=password
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

volumes:
  mongo_data:
  rabbitmq_data: